final game = Game(Cardinals(), Cubs());

game.away.setLineup([]);
game.home.setDefence(
  pitcher:
  catcher:
  first:
  second:
  shortStop:
  third:
  left:
  center:
  right:
);
game.inning();


game.atBat(
  batter:
  pitcher:
);
game.pitch(
  type: PitchType.fourSeamFastball,
  velocity: 97.1,
  target: Point(0.014, -0.213),
  result: PitchResult.ball,
);
game.pitch(
  type: PitchType.fourSeamFastball,
  velocity: 96.7,
  target: Point(-0.114, 0.113),
  result: PitchResult.ball,
);
game.pitch(
  type: PitchType.fourSeamFastball,
  velocity: 97.1,
  target: Point(-0.201, 0.213),
  result: PitchResult.ball,
);
game.pitch(
  type: PitchType.fourSeamFastball,
  velocity: 97.1,
  target: Point(4.014, 3.213),
  result: PitchResult.ball,
);


game.atBat(
  batter:
  pitcher:
);
game.pitch(
  type: PitchType.fourSeamFastball,
  result: PitchResult.strike,
);
game.pitch(
  type: PitchType.fourSeamFastball,
  result: PitchResult.strike,
);
game.pitch(
  type: PitchType.fourSeamFastball,
  result: PitchResult.strike,
);


game.atBat(
  batter:
  pitcher:
);
game.pitch(
  type: PitchType.fourSeamFastball,
  result: PitchResult.hit,
  baseReached: second,
  error: true,
);
game.error(player:);

